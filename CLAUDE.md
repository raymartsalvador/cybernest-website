# Project: cybernest-website

## Stack
<!-- Describe your stack here: e.g., Next.js 15, TypeScript, Tailwind, Prisma, PostgreSQL -->

## Build & Run Commands
- `npm install` — install dependencies
- `npm run dev` — start dev server
- `npm run build` — production build
- `npm test` — run tests
- `npm run lint` — run linter

## Architecture
<!-- Key architectural decisions, patterns, data flow -->

## Conventions
- Conventional commits: feat:, fix:, chore:, refactor:, docs:, test:
- Write tests alongside implementation
- Always include loading, empty, and error states for UI components
- Mobile-responsive by default
- Never hardcode API keys or secrets
- Use environment variables for all configuration

## Key Files
<!-- Important file paths and their purpose -->
<!-- e.g., src/app/layout.tsx — root layout -->

## Testing
- Tests live next to source files (*.test.ts, *.spec.ts)
- Run `npm test` before committing
- Integration tests in __tests__/ directory

## Notes
<!-- Project-specific gotchas, workarounds, context -->
